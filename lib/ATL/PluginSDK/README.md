# Vendored public SDK

These modules are copied without changes from atl-terminal-plugin-sdk 0.2.0.
The accompanying LICENSE applies. Keep both Package.pm and Context.pm identical
to the canonical SDK on updates; do not maintain a separate validator here.

They implement bounded data validation/selection, not ATL Terminal's private core.
