# Logo Policy

This repository starts with text badges instead of bundled third-party artwork.

Rules:

- `logo.txt` is safe for public examples and curated plugins.
- Binary logo assets are optional.
- Do not fetch logos at runtime.
- Third-party artwork must include source and usage notes before it is merged.
- ATL Terminal should fall back to a local text badge when a logo is missing.
