# Docker Diagnostics

Inspect existing containers only. First discover the CLI and current context, then list a small status snapshot. Select a container from observed output before requesting finite logs. Keep the investigation tied to that target. A failed CLI or daemon connection must be reported, not replaced with guessed output. Stop when the user's question is answered.

Requires user intent, confirmed target, and fresh output before follow-up actions.
